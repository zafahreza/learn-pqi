import RootLayout from "@/app/layout"

export default function SignUpDosen(){
    const pageTitle = "LearnPQI - Sign Up Dosen"
    return(
        <RootLayout title={pageTitle}>
        <main className="flex justify-center items-center max-w-screen min-h-screen">
            <p className="text-9xl font-Poppins font-black">Coming Soon!</p>
        </main>
        </RootLayout>
    )
}